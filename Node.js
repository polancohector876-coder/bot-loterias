# Entra a la carpeta de tu proyecto (reemplaza "banca" por la ruta de tu carpeta)
cd ~/Desktop/banca

# Inicializa npm si no lo tienes hecho
npm init -y

# Instala el CLI de Capacitor y el núcleo
npm install @capacitor/core
npm install -D @capacitor/cli